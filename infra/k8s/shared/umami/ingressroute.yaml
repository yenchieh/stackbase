# Dashboard at http://umami.test via the shared Traefik (cross-namespace).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: umami
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`umami.test`)
      kind: Rule
      services:
        - name: umami
          port: 3000
